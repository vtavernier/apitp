{
  "name": "APITP",
  "description": "APITP Project management application",
  "repository": "https://github.com/vtavernier/apitp",
  "scripts": {
    "dokku": {
      "plugins": [
        "postgres"
      ],
      "predeploy": "bundle exec rake db:migrate"
    }
  }
}
