<h2><%= @project.display_name %></h2>

<p><%= @user.name %>, you have been added to the project <%= link_to @project.display_name, project_url(id: @project.id) %></p>

<p>This project is due by <%= l @project.end_time, format: :long %>.</p>

<% unless @project.blank? %>
<p>The project material is located at <%= link_to @project.url, @project.url %></p>
<% end %>

<% admin = @project.assignment_group(@user).admin %>
<p>
  If you have any questions, feel free to contact your project administrator:
  <%= link_to admin.name_email, "mailto:#{admin.email}" %>.
</p>
