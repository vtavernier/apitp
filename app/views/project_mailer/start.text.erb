<%= @project.display_name %>
===================

<%= @user.name %>, you have been added to the project <%= @project.display_name %>.

You can access the project on APITP at the following address:

    <%= project_url(id: @project.id) %>

This project is due by <%= l @project.end_time, format: :long %>.

<% unless @project.blank? %>
The project material is located at <%= @project.url %>
<% end %>

<%= render 'admin_contact', locals: { admin: @project.assignment_group(@user).admin } %>
