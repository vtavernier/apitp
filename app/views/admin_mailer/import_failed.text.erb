Import of <%= @file_name %> failed
===================

<%= @user.name %>, the file you tried to import into APITP could not be imported.

Please ensure the submitted file contains the required fields (name, email, group 1, group 2, etc.)

Here are the exception details:

  <%= @exception.message %>
  Backtrace: <% @exception.backtrace.each do |item| %>
    - <%= item %>
  <% end %>
